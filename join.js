const palavras = ['Olá', 'como', 'vai', 'você?']
const frase = palavras.join(' ')

// o join recebe um array completo e o transforma em uma string, o exmeplo acima mostra com facilidade o seu uso
// toda vez que o join for usado a variavel deixa de ser um array e se torna apenas uma string

console.log(frase)